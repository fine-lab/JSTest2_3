viewModel.get("btnSave") &&
  viewModel.get("btnSave").on("click", function (data) {
    // 保存--单击
    cb.utils.alert("初始化sdgsdgsdgs");
  });
viewModel.on("customInit", function (data) {
  // 函数测试详情--页面初始化
});