viewModel.on("customInit", function (data) {
  // 主子切换_zhang详情--页面初始化
});
viewModel.get("button21ce") &&
  viewModel.get("button21ce").on("click", function (data) {
    // 测试1--单击
    var gridModel = viewModel.getGridModel();
    return false;
  });
viewModel.get("button23re") &&
  viewModel.get("button23re").on("click", function (data) {
    // 保存为草稿--单击
  });